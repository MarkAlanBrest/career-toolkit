const startDate = new Date();
const endDate = new Date(startDate);
endDate.setFullYear(endDate.getFullYear() + 1);

const record = await prisma.courseRecords.create({
  data: {
    FirstName: firstName,
    LastName: lastName,
    Email: email,
    CourseName: courseFolder,
    Code: courseCode,

    StartDate: startDate,
    EndDate: endDate,

    Test1: 0,
    Test2: 0,
    Test3: 0,
    Test4: 0,
    Test5: 0,
    Test6: 0,
    Test7: 0,
    Test8: 0,

    Progress: 0,

    SlidesPath: courseFolder // or null if you prefer
  }
});