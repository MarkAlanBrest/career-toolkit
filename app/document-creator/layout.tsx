export const metadata = {
  title: 'Document Creator',
  description: 'AI-powered professional document creation for educators.',
};

export default function DocumentCreatorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
